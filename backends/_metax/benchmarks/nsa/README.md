# NSA benchmark

`upstream_compare_reference.py` is retained as historical source and contains
mixed-provider comparison logic. It is not imported by the baseline package.
The independent NSA shape matrix is recorded in `shapes.json`.
