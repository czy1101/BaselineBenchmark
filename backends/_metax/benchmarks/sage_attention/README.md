# SageAttention benchmark

The benchmark calls only the externally installed official MetaX
SageAttention wheel through the thin adapter. Default configuration is FP16,
NHD, warmup 25 and 100 repetitions.
